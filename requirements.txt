# ============================================================
# image_text_tool — requirements.txt
# Python 3.10+（推荐 3.10 / 3.11）
# 安装：pip install -r requirements.txt
# ============================================================

# ── GUI 框架 ─────────────────────────────────────────────────
PyQt5>=5.15.0

# ── 系统监控（系统总览页资源监控） ───────────────────────────
psutil>=5.9.0

# ── 截图热键 / 剪贴板（截图工具用 keyboard 注册热键；
#    速存图文 / 粘贴助手用 pyperclip 读写剪贴板） ─────────────
keyboard>=0.13.5
pyperclip>=1.8.0

# ── 网络请求（抖音下载、时区汇率实时汇率、速存插件本地服务等） ─
requests>=2.28.0
urllib3>=1.26.0

# ── 图像处理（抖音下载：图文帖封面图处理） ────────────────────
Pillow>=9.0.0

# ── Ollama 联网搜索（Ollama 工具 / 文学写作，可选，任选其一即可） ─
# ddgs 是新版包名；duckduckgo-search 是旧版，代码两者都能识别
ddgs>=0.1.0
# duckduckgo-search>=6.0.0   # 若 ddgs 安装失败可改用此行

# ── 本地服务（速存图文浏览器插件通信接口） ────────────────────
flask>=2.3.0
flask-cors>=4.0.0

# ── Windows 专属（非 Windows 平台自动忽略） ──────────────────
# 速存图文的前台窗口/资源管理器识别（win32gui / win32con / win32com / pythoncom）
pywin32>=306; sys_platform == "win32"

# ============================================================
# 推荐安装（不装不影响主程序启动，但以下三处功能会降级为纯文字提示）
#   · 导演台页面（page_director.py，故事→制作板/角色/镜头提示词）
#   · 目录映射页的磁盘空间 Treemap 可视化（disk_treemap_widget.py）
#   · 系统总览页的版本日志渲染（page_overview.py）
# ============================================================
PyQtWebEngine>=5.15.0

# ── 可选：系统总览页把版本日志 Markdown 渲染成 HTML 预览 ─────
# 不装则自动用 QTextBrowser 兜底显示纯文本
# markdown>=3.4
